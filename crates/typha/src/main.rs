//! `typha` binary — part of Calico-rs (idiomatic Rust rewrite of Calico).

fn main() {
    eprintln!("typha: not yet implemented (see specs/001-calico-rs-rust-rewrite/tasks.md)");
    std::process::exit(1);
}
